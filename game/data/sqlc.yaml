version: "2"
sql:
  - engine: "sqlite"
    schema: "schema"
    queries: "queries"
    gen:
      go:
        package: "gen"
        out: "./gen"
        emit_interface: true
        emit_exact_table_names: false
